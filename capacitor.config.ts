import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.demareclinicamedica.com',
  appName: 'demare_clinicamedica',
  webDir: 'dist/clinicamedica/browser'
};

export default config;
